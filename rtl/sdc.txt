# 1. Definisanje glavnog takta na portu 'clk' (50 MHz -> 20.000 ns)
create_clock -name sys_clk -period 20.000 [get_ports clk]

# 2. Automatsko izvođenje taktova ako sistem koristi PLL 
derive_pll_clocks

# 3.
derive_clock_uncertainty

# 4. 
set_false_path -from [get_ports {rst}] -to *